trigger:
    - id: Cleanup
      type: job
      config:
        name: cleanup

    - id: refreshdb
      type: Job
      config:
        Name: somejob

    - id: build
      type: WebHook
      config:
        url: http://localhost:8080
        timeout: 1s

    - id: dummy
      type: dummyerror
