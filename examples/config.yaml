web:
    enable: true
    listen-addr: ":8080"

logging:
    threshold: "info"
    verbose: 3

scanner:
    - namespace:
        - "hco-vvandam"
      default:
        schedule:
          - "Mon-Fri  8:00 replicas=1"
          - "Mon-Fri 18:00 replicas=0"
      deployment:
        - selector:
            - "app=shell"
            - "app=nightshift"
          schedule:
          - ""
